const URL: string = 'http://localhost:3000';
export const EVENT_POST_URL = `${URL}/events`;
export const PAGE_VIEW_POST_URL = `${URL}/page-views`;

export const COOKIE_NAME: string = '_buddy_id';

export const MAX_PAYLOAD_SIZE: number = 5;